# Core dependencies for all five stages (rag-1 ... rag-3b).
# Usage with a plain venv:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)  |  source .venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
chromadb>=0.5
openai>=1.40
python-dotenv>=1.0

# Optional — only for notebooks/01_rag-zu-fuss.ipynb:
# jupyterlab>=4.0
# ipykernel>=6.0
