# Core dependencies for all workshop stages.
# Usage with a plain venv:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)  |  source .venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
chromadb>=0.5
openai>=1.40
pypdf>=5.0
python-dotenv>=1.0
sentence-transformers>=5.0
